# 實行紀錄：修復踢人與離線帳號清理資料庫權限 Bug

本份文件紀錄了為解決管理員執行強制登出 (`🚷`) 與幽靈帳號自動清除時，遭到 Firebase 伺服器以 `PERMISSION_DENIED` 拒絕寫入的問題，所進行的資料庫安全規則補全成果。

## 執行變更

### 資料庫安全規則 (`database.rules.json`)
1. **解鎖管理員覆寫權限**：
   - 在 `/users/$uid` 及 `/presence/$uid` 的 `.write` 規則中加入管理員 (`wube8816@gmail.com`) 的覆寫許可。
   - 確保背景自動清理程式能夠順暢地將因為網頁不正常關閉而殘留在資料庫裡的幽靈節點予以抹除。
2. **開通踢人通訊管道**：
   - 於 `/admin` 節點下新增 `kicklist` 專用通訊規則。
   - 允許所有人讀取被踢名單，並賦予管理員發起踢出寫入，以及被踢者自身在強制登出時刪除被踢紀錄的完整權限。

## 驗證結果
- 經由 GitHub Actions 順利完成雲端建置並同步更新至 Firebase Realtime Database。
- 點擊踢人按鈕不再觸發權限報錯，操作順暢且即時生效。
